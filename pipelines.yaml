config:
  argocd:
    namespace: argocd
  pipelines:
    name: test-cicd
environments:
- apps:
  - name: app-taxi
    services:
    - taxi
  name: test-dev
  pipelines:
    integration:
      bindings:
      - github-push-binding
      template: app-ci-template
  services:
  - name: taxi
    pipelines:
      integration:
        bindings:
        - test-dev-taxi-binding
        - github-push-binding
    source_url: https://github.com/ishitasequeira/taxi.git
    webhook:
      secret:
        name: webhook-secret-test-dev-taxi
        namespace: test-cicd
- name: test-stage
gitops_url: https://github.com/ishitasequeira/test.git
