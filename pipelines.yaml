config:
  pipelines:
    name: t1-cicd
environments:
- apps:
  - name: ty
    services:
    - bus4
  name: prod
  pipelines:
    integration:
      bindings:
      - github-pr-binding
      template: app-ci-template
  services:
  - name: bus4
    source_url: https://github.com/ishitasequeira/taxi.git
    webhook:
      secret:
        name: webhook-secret-prod-bus4
        namespace: t1-cicd
gitops_url: https://github.com/ishitasequeira/test.git
